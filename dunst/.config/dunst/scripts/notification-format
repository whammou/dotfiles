#!/usr/bin/env sh

notificationTitle=$DUNST_SUMMARY
notificationBody=$DUNST_BODY

formatedBody=$(echo "$DUNST_BODY" | sed 's/\([^:]*\):/<b>\1<\/b>:/')

dunstify -c "ntfy" "$DUNST_SUMMARY" "$formatedBody"
