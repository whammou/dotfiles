/*
 * ROFI theme entry point — fzf-lua-mimic inspired
 *
 * Imports palette + component partials.
 * Edit the individual .rasi files to tweak each section.
 * Switch themes by changing this file or importing a different palette.
 */

configuration {
  matching: "fuzzy";
  sort: true;
  sorting-method: "fzf";

  drun {
    display-name: "";
  }

  run {
    display-name: "";
  }

  window {
    display-name: "";
  }

  timeout {
    action: "kb-cancel";
  }
}

@import "palette"
@import "window"
@import "inputbar"
@import "elements"
@import "scrollbar"