[Unit]
Description=qspawn daemon — persistent qtile IPC (merged)
After=qtile-session.target
PartOf=qtile-session.target

[Service]
ExecStart=%h/.local/bin/qspawn --daemon
Restart=always
RestartSec=1
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=qtile-session.target
